<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/user_tab"
        android:enabled="true"
        android:icon="@drawable/ic_my_diary_tab"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/workout_tab"
        android:enabled="true"
        android:icon="@drawable/ic_workout_tab"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/diary_tab"
        android:enabled="true"
        android:icon="@drawable/ic_nutrition_tab"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/nutrition_tab"
        android:enabled="true"
        android:icon="@drawable/ic_user_tab"
        tools:ignore="MenuTitle" />
</menu>